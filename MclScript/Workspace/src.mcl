# this is a comment

main()
{
    x = 0;
    while (x < 10)
    {
        x = x + 1;
        if (x : x) { print(x); }
    }
    
    print(9999999);
    
    a = 1; b = 2; c = 3; # comment
    
    d = func(0, a, b) + ((5 + (420 - 0) * c 
            * (func((a + b) * c + 3, 1+2-7, 69)))) + 1;
    
    y = input();
    if (y > 0) { print(y); } # comment
    else (1) { print(-100); }
    
    print(d);
}

# comment
func(x, y, z)
{
    if (z > 0) { # comment
        return = (x + y + z);
    }
    else (1) {
        return = ((x+y)-z);
    }
}